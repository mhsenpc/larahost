server {
        listen 80;
        listen [::]:80;

        server_name $project_name.gnulover.ir www.$project_name.gnulover.ir;

        location / {
                proxy_pass http://127.0.0.1:$port;
        }
}
